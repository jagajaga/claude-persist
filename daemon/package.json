{
  "name": "@claude-persist/daemon",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p .",
    "test": "node --test dist/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "*",
    "@claude-persist/shared": "0.1.0"
  }
}
