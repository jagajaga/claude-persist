{
  "name": "claude-persist-vscode",
  "displayName": "Claude Persist",
  "description": "Persistent Claude Code sessions that survive window reloads — chat tabs backed by a server-side daemon (code-server friendly)",
  "version": "1.0.38",
  "publisher": "jaga",
  "license": "MIT",
  "icon": "media/ext-icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/jagajaga/claude-persist.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "AI",
    "Chat"
  ],
  "keywords": [
    "claude",
    "claude code",
    "anthropic",
    "agent",
    "chat",
    "code-server",
    "remote development",
    "persistent sessions"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false,
      "description": "Claude Persist runs Claude Code against the files in this folder, so the folder has to be trusted."
    },
    "virtualWorkspaces": {
      "supported": false,
      "description": "Sessions run in a real working directory on the machine hosting the daemon."
    }
  },
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "claudePersist",
          "title": "Claude Persist",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "claudePersist": [
        {
          "id": "claudePersist.sessions",
          "name": "Sessions",
          "icon": "media/icon.svg",
          "contextualTitle": "Claude Persist"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "claudePersist.sessions",
        "contents": "No Claude sessions yet.\n[New Session](command:claudePersist.newSession)\nSessions run in a server-side daemon and survive window reloads.\nNot signed in to Claude yet? Create a session, then use [Add Account](command:claudePersist.addAccount)."
      }
    ],
    "commands": [
      {
        "command": "claudePersist.addAccount",
        "title": "Add Account (Sign In)",
        "category": "Claude Persist"
      },
      {
        "command": "claudePersist.newSession",
        "title": "New Session",
        "icon": "$(add)",
        "category": "Claude Persist"
      },
      {
        "command": "claudePersist.openSession",
        "title": "Open Session",
        "category": "Claude Persist"
      },
      {
        "command": "claudePersist.deleteSession",
        "title": "Delete Session",
        "category": "Claude Persist"
      },
      {
        "command": "claudePersist.refreshSessions",
        "title": "Refresh Sessions",
        "icon": "$(refresh)",
        "category": "Claude Persist"
      },
      {
        "command": "claudePersist.importSession",
        "title": "Import Claude Code Session",
        "icon": "$(cloud-download)",
        "category": "Claude Persist"
      },
      {
        "command": "claudePersist.renameSession",
        "title": "Rename Session",
        "icon": "$(edit)",
        "category": "Claude Persist"
      },
      {
        "command": "claudePersist.openSessionFromTree",
        "title": "Open Session"
      },
      {
        "command": "claudePersist.deleteSessionItem",
        "title": "Delete Session",
        "icon": "$(trash)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "claudePersist.newSession",
          "when": "view == claudePersist.sessions",
          "group": "navigation@1"
        },
        {
          "command": "claudePersist.refreshSessions",
          "when": "view == claudePersist.sessions",
          "group": "navigation@2"
        },
        {
          "command": "claudePersist.importSession",
          "when": "view == claudePersist.sessions",
          "group": "1_actions"
        }
      ],
      "view/item/context": [
        {
          "command": "claudePersist.renameSession",
          "when": "view == claudePersist.sessions && viewItem == claudeSession",
          "group": "inline@1"
        },
        {
          "command": "claudePersist.deleteSessionItem",
          "when": "view == claudePersist.sessions && viewItem == claudeSession",
          "group": "inline@2"
        }
      ],
      "commandPalette": [
        {
          "command": "claudePersist.openSessionFromTree",
          "when": "false"
        },
        {
          "command": "claudePersist.deleteSessionItem",
          "when": "false"
        }
      ]
    },
    "configuration": {
      "title": "Claude Persist",
      "properties": {
        "claudePersist.daemonEntry": {
          "type": "string",
          "default": "",
          "description": "Absolute path to the daemon entry point (daemon/dist/main.js). Leave empty to auto-detect the monorepo layout next to the extension."
        },
        "claudePersist.extraModels": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Extra model ids merged into the model dropdown (e.g. claude-opus-5) — for models the SDK's probed list doesn't include yet. Entries the SDK already lists are ignored."
        },
        "claudePersist.switchAccountOnLimit": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "When a plan rate limit is hit, switch to the next logged-in account (the list wraps) and continue automatically. When every account is limited, wait for whichever limit ends soonest and continue then.\n\nSwitching is daemon-wide, so it moves **every** session to the new account. Each switch also copies the session transcript into the new account's directory and starts with a cold prompt cache, so the first turn after a switch costs more.\n\nTurn this off to keep work on one account and simply wait for its limit to reset."
        },
        "claudePersist.connectionIndicator": {
          "type": "boolean",
          "default": true,
          "description": "Blink a border around the chat when it loses contact with the server — the browser connection or the daemon. Detected by a round-trip heartbeat, not by guessing from silence."
        },
        "claudePersist.defaultModel": {
          "type": "string",
          "default": "",
          "description": "Model id applied to every new or imported session (e.g. claude-opus-5). Leave empty to use the account default."
        }
      }
    },
    "walkthroughs": [
      {
        "id": "claudePersist.gettingStarted",
        "title": "Get started with Claude Persist",
        "description": "Chat with Claude in sessions that survive window reloads.",
        "steps": [
          {
            "id": "session",
            "title": "Create a session",
            "description": "Pick a folder to work in. The session runs in a background daemon, not in this window.\n[Create a session](command:claudePersist.newSession)",
            "media": {
              "markdown": "media/walkthrough/session.md"
            },
            "completionEvents": [
              "onCommand:claudePersist.newSession"
            ]
          },
          {
            "id": "signin",
            "title": "Sign in to Claude",
            "description": "Only if you have never used Claude Code on this machine. No terminal needed — the panel gives you a link and a box for the code.\n[Add an account](command:claudePersist.addAccount)",
            "media": {
              "markdown": "media/walkthrough/signin.md"
            },
            "completionEvents": [
              "onCommand:claudePersist.addAccount"
            ]
          },
          {
            "id": "reload",
            "title": "Reload the window mid-answer",
            "description": "The turn keeps running in the daemon and the tab replays everything it missed. That is the whole point.",
            "media": {
              "markdown": "media/walkthrough/reload.md"
            }
          }
        ]
      }
    ]
  },
  "scripts": {
    "build": "tsc -p ."
  },
  "devDependencies": {
    "@claude-persist/shared": "0.1.0",
    "@types/vscode": "^1.85.0"
  },
  "homepage": "https://github.com/jagajaga/claude-persist#readme",
  "bugs": {
    "url": "https://github.com/jagajaga/claude-persist/issues"
  },
  "qna": "https://github.com/jagajaga/claude-persist/issues",
  "galleryBanner": {
    "color": "#1f1f1f",
    "theme": "dark"
  }
}
